# 基础镜像
FROM openjdk:8-jre-slim

# 设置工作目录
WORKDIR /app

# 复制 JAR 包到容器
COPY target/business-behavior-monitor-app.jar /app/business-behavior-monitor-app.jar

# 运行 JAR 包
CMD ["java", "-jar", "/app/business-behavior-monitor-app.jar"]
